# Hadolint configuration
# https://github.com/hadolint/hadolint

ignored:
  # DL3041: dnf version pinning — Fedora packages roll continuously,
  # pinning creates maintenance burden without real reproducibility
  - DL3041
