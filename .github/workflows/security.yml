name: Dependency security scan

on:
  pull_request:
  schedule:
    # Weekly Monday 08:00 UTC
    - cron: "0 8 * * 1"

permissions:
  contents: read

# Python dependency security scans for ragpipe and ragstuffer
# run in their own repos:
# - github.com/aclater/ragpipe
# - github.com/aclater/ragstuffer

jobs:
  placeholder:
    name: No local Python dependencies
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security scans for ragpipe and ragstuffer run in their own repos"
